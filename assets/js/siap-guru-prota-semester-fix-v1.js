(()=>{
/* PROTA SEMESTER SAFETY LOCK
   This legacy auto-split routine intentionally stays disabled.
   PROTA semester values must come from the user's saved/manual data and
   must never be rewritten automatically by a MutationObserver. */
window.__sgProtaSemesterFixDisabled=true;
})();
