/* SIAP GURU — Master BAB/Topik v2
   Bank kelas 1-6. Generator lokal, tanpa token/API AI.
   Judul yang belum diverifikasi langsung dari isi buku memakai nomor BAB.
*/
(()=>{const KEY='siapguru_master_bab_v1';
const makeSeed=()=>{
 const out=[],add=(mapel,kelas,count,source,jp=8)=>{const fase=kelas<=2?'A':kelas<=4?'B':'C';for(let n=1;n<=count;n++)out.push({id:`seed-${mapel.replace(/\W/g,'').toLowerCase()}-${kelas}-${n}`,mapel,fase,kelas,semester:n<=Math.ceil(count/2)?1:2,bab:`Bab ${n}`,subtopik:[],jp,source,sourceUrl:'https://buku.kemendikdasmen.go.id/katalog'});};
 const general=[['Bahasa Indonesia',8,'Bahasa Indonesia — SIBI'],['Pendidikan Pancasila',4,'Pendidikan Pancasila — SIBI'],['Matematika',8,'Matematika — SIBI'],['PJOK',8,'Pendidikan Jasmani, Olahraga, dan Kesehatan — SIBI'],['Seni Musik',6,'Seni Musik — SIBI'],['Seni Rupa',6,'Seni Rupa — SIBI']];
 for(let k=1;k<=6;k++)general.forEach(([m,c,s])=>add(m,k,c,s));for(let k=3;k<=6;k++)add('IPAS',k,8,'Ilmu Pengetahuan Alam dan Sosial — SIBI');
 ['Pendidikan Agama Islam dan Budi Pekerti','Pendidikan Agama Kristen dan Budi Pekerti','Pendidikan Agama Katolik dan Budi Pekerti','Pendidikan Agama Hindu dan Budi Pekerti','Pendidikan Agama Buddha dan Budi Pekerti','Pendidikan Agama Khonghucu dan Budi Pekerti'].forEach(m=>{for(let k=1;k<=6;k++)add(m,k,10,`SIBI — ${m}`,6)});
 const bi=['Sudah Besar','Di Bawah Atap','Lihat Sekitar','Meliuk dan Menerjang','Bertukar dan Membayar','Satu Titik','Asal-Usul','Sehatlah Ragaku'];bi.forEach((t,i)=>{const x=out.find(a=>a.mapel==='Bahasa Indonesia'&&a.kelas===4&&a.id.endsWith(`-${i+1}`));if(x)x.bab=`Bab ${i+1} — ${t}`});return out;
};
const seed=makeSeed();const load=()=>{try{const old=JSON.parse(localStorage.getItem(KEY)||'null');if(!Array.isArray(old))return seed;const map=new Map(seed.map(x=>[x.id,x]));old.forEach(x=>{if(x&&x.id)map.set(x.id,x)});return [...map.values()]}catch{return seed}};let data=load();const save=()=>localStorage.setItem(KEY,JSON.stringify(data));const phase=k=>k<=2?'A':k<=4?'B':'C';const qs=s=>String(s||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function open(){let v=document.getElementById('sgMasterBabView');if(!v){v=document.createElement('section');v.id='sgMasterBabView';v.className='inner-view';v.hidden=true;document.querySelector('.main-content')?.appendChild(v)}v.hidden=false;document.querySelectorAll('.main-content>section').forEach(x=>{if(x!==v)x.hidden=true});render(v)}
function render(v){const mapels=[...new Set(data.map(x=>x.mapel))].sort();v.innerHTML=`<div class="sg-master-bab-room"><div class="inner-view-head"><div><button class="back-button" type="button" data-sg-master-back>← Beranda</button><span class="eyebrow">PEMBELAJARAN</span><h1>Master BAB / Topik</h1><p>Bank BAB kelas 1–6 untuk Generate Otomatis Pembelajaran Mendalam.</p></div><div class="teacher-chip"><strong>${data.length}</strong><span>topik tersimpan</span></div></div><div class="sg-master-toolbar"><select id="sgMBMapel"><option value="">Semua mata pelajaran</option>${mapels.map(x=>`<option>${qs(x)}</option>`).join('')}</select><select id="sgMBKelas"><option value="">Semua kelas</option>${[1,2,3,4,5,6].map(x=>`<option value="${x}">Kelas ${x}</option>`).join('')}</select><select id="sgMBSem"><option value="">Semua semester</option><option value="1">Semester 1</option><option value="2">Semester 2</option></select><input id="sgMBCari" type="search" placeholder="Cari BAB / topik..."><button id="sgMBAdd" class="sg-export-primary" type="button">+ Tambah BAB</button></div><div id="sgMBList"></div><div class="sg-master-note"><strong>Sumber:</strong> katalog SIBI. Judul yang belum diverifikasi langsung dari isi buku sengaja memakai nomor BAB, bukan karangan.</div></div>`;const renderList=()=>{const m=document.getElementById('sgMBMapel')?.value,k=document.getElementById('sgMBKelas')?.value,s=document.getElementById('sgMBSem')?.value,q=(document.getElementById('sgMBCari')?.value||'').toLowerCase();const rows=data.filter(x=>(!m||x.mapel===m)&&(!k||String(x.kelas)===k)&&(!s||String(x.semester)===s)&&(!q||`${x.bab} ${(x.subtopik||[]).join(' ')}`.toLowerCase().includes(q)));document.getElementById('sgMBList').innerHTML=rows.length?`<div class="sg-master-table-wrap"><table class="sg-master-table"><thead><tr><th>BAB / TOPIK</th><th>Mapel</th><th>Kelas</th><th>Semester</th><th>JP</th><th>Sumber</th><th></th></tr></thead><tbody>${rows.map(x=>`<tr><td><strong>${qs(x.bab)}</strong><small>${qs((x.subtopik||[]).join(' • ')||'Topik dapat dikembangkan dari TP')}</small></td><td>${qs(x.mapel)}</td><td>${x.kelas}</td><td>${x.semester}</td><td>${x.jp}</td><td>${qs(x.source||'-')}</td><td><button type="button" class="sg-master-use" data-id="${x.id}">Gunakan</button></td></tr>`).join('')}</tbody></table></div>`:'<div class="empty-state">Belum ada BAB yang sesuai filter.</div>';document.querySelectorAll('.sg-master-use').forEach(b=>b.onclick=()=>use(b.dataset.id))};['sgMBMapel','sgMBKelas','sgMBSem','sgMBCari'].forEach(id=>document.getElementById(id)?.addEventListener('input',renderList));document.getElementById('sgMBAdd').onclick=()=>add(renderList);document.querySelector('[data-sg-master-back]')?.addEventListener('click',()=>{v.hidden=true;document.getElementById('homeView')?.removeAttribute('hidden')});renderList()}
function add(refresh){const mapel=prompt('Mata pelajaran:','Matematika');if(!mapel)return;const kelas=Number(prompt('Kelas 1-6:','4'));if(kelas<1||kelas>6)return;const semester=Number(prompt('Semester 1/2:','1'));if(![1,2].includes(semester))return;const bab=prompt('Nama BAB / Topik:');if(!bab)return;const jp=Number(prompt('Alokasi JP:','8'))||8;data.push({id:'custom-'+Date.now(),mapel,fase:phase(kelas),kelas,semester,bab,subtopik:[],jp,source:'Ditambahkan guru'});save();refresh()}
function use(id){const x=data.find(a=>a.id===id);if(!x)return;sessionStorage.setItem('siapguru_selected_topic',JSON.stringify(x));document.dispatchEvent(new CustomEvent('siapguru:topic-selected',{detail:x}));alert(`BAB/Topik dipilih: ${x.bab}\n\nKelas ${x.kelas} • ${x.mapel}\nTopik siap dipakai oleh generator Pembelajaran Mendalam.`)}
window.SiapGuruMasterBab={open,getAll:()=>data,select:use};document.addEventListener('click',e=>{const a=e.target.closest?.('.sg-topnav-link');if(a&&a.textContent.trim()==='Master BAB / Topik'){e.preventDefault();e.stopPropagation();open()}},true);document.addEventListener('DOMContentLoaded',()=>setTimeout(()=>{const sub=[...document.querySelectorAll('.sub-menu span,.sub-menu-button')];if(!sub.some(x=>x.textContent.trim()==='Master BAB / Topik')){const card=[...document.querySelectorAll('.menu-card')].find(x=>x.querySelector('h3')?.textContent.trim()==='Pembelajaran');const sm=card?.querySelector('.sub-menu');if(sm){const s=document.createElement('span');s.textContent='Master BAB / Topik';sm.appendChild(s)}}},150));})();