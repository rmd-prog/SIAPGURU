/* RPM route guard intentionally disabled.
   The canonical RPM click handler lives in siap-guru-rpm.js.
   Keeping this file inert prevents duplicate capture handlers from blocking it. */
(()=>{})();
